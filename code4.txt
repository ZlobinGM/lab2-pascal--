program Example;
const
    Pi: double = 3.1415926;
    one: integer = 1;
var
    num1, num2: integer;
    Res, d: double;
begin
    num1 := one + one + one;
    Write("Введите целое неотрицательное число: ");
    Read(num2);
    Write("Введите вещественное число: ");
    Read(d);
    if (num2 + 100 mod 6 >= 50 / 25 * 2) then
    begin
        Res := num1 * num2 + d;
        Write("Результат = ", Res);
    end
    else
    begin
        Write("Неверный ввод");
    end
end.